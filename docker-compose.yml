# Mariadb 
version: '3.7'

services:
  mariadb:
    image: mariadb:10.4
    container_name: mariadb
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=123456
    ports:
      - 3306:3306
